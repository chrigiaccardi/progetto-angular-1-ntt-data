import { PipeStatoPipe } from './pipe-stato-pipe';

describe('PipeStatoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeStatoPipe();
    expect(pipe).toBeTruthy();
  });
});
